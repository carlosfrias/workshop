# Phase 0 Decomposition — B-KR-001: Implement Persistent Kill-Switch (v2 — Multi-Node Parallel)

**Date:** 2026-05-13  
**Phase:** Phase 0  
**Backlog Item:** B-KR-001  
**Decomposed by:** High Cloud Model (`ollama-cloud/kimi-k2.6`)  
**Handoff to:** Low Cloud Model (`ollama-cloud/qwen3.5:397b`)  
**Skill:** `decompose-execute-verify`  
**Reference:** [`AGENTS.md`](./AGENTS.md)

---

## [S-TIGHT]

B-KR-001 decomposed into 20 local-model-sized sub-steps grouped into 4 waves. **Wave 0 is sequential:** scaffold the test harness. **Waves 1–3 run in parallel** across the lab node pool (fnet1–fnet7). Each wave contains 5–7 sub-steps that can run concurrently on different nodes. Total estimated effort: 2.5 hours, wall-clock time: ~45 minutes with full parallelism.

---

## Parallel Execution Overview

```
Wave 0 (Sequential)          Wave 1 (Parallel)              Wave 2 (Parallel)              Wave 3 (Parallel)
┌──────────────────┐        ┌──────────────────┐           ┌──────────────────┐           ┌──────────────────┐
│ HARNESS-001      │   →    │ UNIT-001A        │   →       │ INT-001A         │   →       │ ACC-001A         │
│ HARNESS-002      │        │ UNIT-001B        │           │ INT-001B         │           │ ACC-001B         │
│ HARNESS-003      │        │ UNIT-002A        │           │ INT-002A         │           │ ACC-001C         │
│                  │        │ UNIT-002B        │           │ INT-002B         │           │ DOC-001A         │
│                  │        │ UNIT-003A        │           │                  │           │ DOC-001B         │
│                  │        │ UNIT-003B        │           │                  │           │                  │
│                  │        │ UNIT-003C        │           │                  │           │                  │
└──────────────────┘        └──────────────────┘           └──────────────────┘           └──────────────────┘
       fnet3                 fnet1  fnet2  fnet7             fnet3  fnet4  fnet5             fnet6  fnet3  fnet1
```

**Dependency rule:** Nothing in Wave N+1 starts until **all** Wave N steps are complete AND verified.

---

## Wave 0 — Harness Scaffold (Sequential, Prerequisite)

### Step 0A: B-KR-001-HARNESS-001 — Create Test Directories

| Field | Value |
|-------|-------|
| `step_id` | `B-KR-001-HARNESS-001` |
| `title` | Create test/unit/, test/integration/, test/acceptance/ directories |
| `workspace` | `pi-keyword-router` |
| `target_file` | `technical-infrastructure/packages/pi-keyword-router/test/` |
| `target_node` | **fnet3** |
| `acceptance_criteria` | Three directories exist: test/unit/, test/integration/, test/acceptance/ |
| `estimated_effort` | 5 min |
| `recommended_model` | `low-local` |

### Step 0B: B-KR-001-HARNESS-002 — Create Test Runner Config

| Field | Value |
|-------|-------|
| `step_id` | `B-KR-001-HARNESS-002` |
| `title` | Create jest.config.js or vitest.config.ts for the test runner |
| `workspace` | `pi-keyword-router` |
| `target_file` | `technical-infrastructure/packages/pi-keyword-router/jest.config.js` |
| `target_node` | **fnet3** (same session as 0A — runs immediately after) |
| `acceptance_criteria` | Config file exists with testMatch patterns for unit/, integration/, acceptance/. Config references the existing routing-transparency runner config as template. |
| `estimated_effort` | 10 min |
| `recommended_model` | `low-local` |

### Step 0C: B-KR-001-HARNESS-003 — Wire package.json Test Script

| Field | Value |
|-------|-------|
| `step_id` | `B-KR-001-HARNESS-003` |
| `title` | Add "test" script to package.json and verify npm test runs |
| `workspace` | `pi-keyword-router` |
| `target_file` | `technical-infrastructure/packages/pi-keyword-router/package.json` |
| `target_node` | **fnet3** (same session as 0A/0B) |
| `acceptance_criteria` | package.json has "test": "jest --config jest.config.js" (or equivalent). `npm test` runs with 0 tests found (passing for empty harness). |
| `estimated_effort` | 5 min |
| `recommended_model` | `low-local` |

**Wave 0 Dispatch (pi-intercom):**
```typescript
intercom({
  action: "ask",
  to: "fnet3",
  message: "Wave 0: Execute HARNESS-001, HARNESS-002, HARNESS-003 in sequence on this node. Create test directories, test runner config, and wire package.json. Verify npm test runs. Report back."
})
```

---

## Wave 1 — Unit Test Stubs (Parallel, 7 sub-steps)

**All Wave 1 sub-steps can run concurrently** on different nodes. The harness exists; each stub is independent.

### Step 1A: B-KR-001-UNIT-001A — Kill-Switch Config Schema: Import + Setup

| Field | Value |
|-------|-------|
| `step_id` | `B-KR-001-UNIT-001A` |
| `title` | Write test file header, imports, and describe block for kill-switch config test |
| `workspace` | `pi-keyword-router` |
| `target_file` | `test/unit/kill-switch.test.ts` |
| `acceptance_criteria` | File exists with `import { loadConfig } from '../lib/config'` and `describe('kill-switch config', () => { ... })`. |
| `target_node` | **fnet1** |
| `estimated_effort` | 5 min |
| `recommended_model` | `low-local` |

### Step 1B: B-KR-001-UNIT-001B — Kill-Switch Config Schema: Assertion

| Field | Value |
|-------|-------|
| `step_id` | `B-KR-001-UNIT-001B` |
| `title` | Write assertion: loadConfig() reads extensions.pi-keyword-router.enabled as boolean |
| `workspace` | `pi-keyword-router` |
| `target_file` | `test/unit/kill-switch.test.ts` (append) |
| `acceptance_criteria` | Test assertion exists: `expect(config.enabled).toBeDefined()` inside an `it('reads enabled flag', ...)` block. Running the test fails because no `enabled` field exists in config schema. |
| `target_node` | **fnet1** (same session as 1A) |
| `estimated_effort` | 5 min |
| `recommended_model` | `low-local` |

### Step 2A: B-KR-001-UNIT-002A — Gatekeeper Bypass: Import + Setup

| Field | Value |
|-------|-------|
| `step_id` | `B-KR-001-UNIT-002A` |
| `title` | Write test file header, imports, and describe block for gatekeeper bypass test |
| `workspace` | `pi-keyword-router` |
| `target_file` | `test/unit/gatekeeper.test.ts` |
| `acceptance_criteria` | File exists with imports and `describe('gatekeeper bypass', () => { ... })`. |
| `target_node` | **fnet2** |
| `estimated_effort` | 5 min |
| `recommended_model` | `low-local` |

### Step 2B: B-KR-001-UNIT-002B — Gatekeeper Bypass: Assertion

| Field | Value |
|-------|-------|
| `step_id` | `B-KR-001-UNIT-002B` |
| `title` | Write assertion: shouldRegisterHooks() returns false when enabled === false |
| `workspace` | `pi-keyword-router` |
| `target_file` | `test/unit/gatekeeper.test.ts` (append) |
| `acceptance_criteria` | Test assertion exists: `expect(shouldRegisterHooks({ enabled: false })).toBe(false)`. Running the test fails because `shouldRegisterHooks` does not exist. |
| `target_node` | **fnet2** (same session as 2A) |
| `estimated_effort` | 5 min |
| `recommended_model` | `low-local` |

### Step 3A: B-KR-001-UNIT-003A — Footer Disabled State: Import + Setup

| Field | Value |
|-------|-------|
| `step_id` | `B-KR-001-UNIT-003A` |
| `title` | Write test file header for routing-transparency footer disabled state test |
| `workspace` | `routing-transparency` |
| `target_file` | `test/unit/footer-disabled-state.test.ts` |
| `acceptance_criteria` | File exists with imports and `describe('footer disabled state', () => { ... })`. |
| `target_node` | **fnet7** |
| `estimated_effort` | 5 min |
| `recommended_model` | `low-local` |

### Step 3B: B-KR-001-UNIT-003B — Footer Disabled State: Render Assertion

| Field | Value |
|-------|-------|
| `step_id` | `B-KR-001-UNIT-003B` |
| `title` | Write assertion: footer renders 'keyword-router: disabled' when enabled === false |
| `workspace` | `routing-transparency` |
| `target_file` | `test/unit/footer-disabled-state.test.ts` (append) |
| `acceptance_criteria` | Test assertion exists: `expect(render({ enabled: false })).toContain('keyword-router: disabled')`. Running the test fails because footer has no disabled state handler. |
| `target_node` | **fnet7** (same session as 3A) |
| `estimated_effort` | 5 min |
| `recommended_model` | `low-local` |

### Step 3C: B-KR-001-UNIT-003C — Footer Disabled State: Source Indicator

| Field | Value |
|-------|-------|
| `step_id` | `B-KR-001-UNIT-003C` |
| `title` | Write second assertion: footer shows src: disabled when keyword-router is off |
| `workspace` | `routing-transparency` |
| `target_file` | `test/unit/footer-disabled-state.test.ts` (append) |
| `acceptance_criteria` | Test assertion exists: `expect(render({ enabled: false })).toContain('src: disabled')`. Running the test fails alongside 003B. |
| `target_node` | **fnet7** (same session as 3A/3B) |
| `estimated_effort` | 5 min |
| `recommended_model` | `low-local` |

**Wave 1 Parallel Dispatch (pi-intercom):**
```typescript
// All dispatched simultaneously to different nodes
intercom({ action: "ask", to: "fnet1", message: "Wave 1: Execute UNIT-001A and UNIT-001B in sequence. Report test output + git diff." })
intercom({ action: "ask", to: "fnet2", message: "Wave 1: Execute UNIT-002A and UNIT-002B in sequence. Report test output + git diff." })
intercom({ action: "ask", to: "fnet7", message: "Wave 1: Execute UNIT-003A, UNIT-003B, and UNIT-003C in sequence. Report test output + git diff." })
```

---

## Wave 2 — Integration Test Stubs (Parallel, 4 sub-steps)

**All Wave 2 sub-steps can run concurrently** on different nodes. They depend only on Wave 0 (harness exists).

### Step 4A: B-KR-001-INT-001A — Model-Router Bypass: Setup + Mock

| Field | Value |
|-------|-------|
| `step_id` | `B-KR-001-INT-001A` |
| `title` | Write integration test setup and mock for keyword-router + model-router bypass |
| `workspace` | `pi-keyword-router` |
| `target_file` | `test/integration/keyword-router-with-model-router.test.ts` |
| `acceptance_criteria` | File exists with mocked model-router event bus and `describe('keyword-router bypass', () => { ... })`. |
| `target_node` | **fnet3** |
| `estimated_effort` | 10 min |
| `recommended_model` | `medium-local` |

### Step 4B: B-KR-001-INT-001B — Model-Router Bypass: Assertion

| Field | Value |
|-------|-------|
| `step_id` | `B-KR-001-INT-001B` |
| `title` | Write assertion: model-router receives fallback route when enabled === false |
| `workspace` | `pi-keyword-router` |
| `target_file` | `test/integration/keyword-router-with-model-router.test.ts` (append) |
| `acceptance_criteria` | Test assertion: `expect(fallbackRoute).toBeCalled()` when `enabled: false`. Running the test fails. |
| `target_node` | **fnet3** (same session as 4A) |
| `estimated_effort` | 10 min |
| `recommended_model` | `medium-local` |

### Step 5A: B-KR-001-INT-002A — Transparency Event Suppression: Setup + Mock

| Field | Value |
|-------|-------|
| `step_id` | `B-KR-001-INT-002A` |
| `title` | Write integration test setup and mock for keyword-router + routing-transparency event suppression |
| `workspace` | `pi-keyword-router` |
| `target_file` | `test/integration/keyword-router-with-transparency.test.ts` |
| `acceptance_criteria` | File exists with mocked event emitter and `describe('event suppression', () => { ... })`. |
| `target_node` | **fnet4** |
| `estimated_effort` | 10 min |
| `recommended_model` | `medium-local` |

### Step 5B: B-KR-001-INT-002B — Transparency Event Suppression: Assertion

| Field | Value |
|-------|-------|
| `step_id` | `B-KR-001-INT-002B` |
| `title` | Write assertion: keyword-router:routed event is NOT emitted when enabled === false |
| `workspace` | `pi-keyword-router` |
| `target_file` | `test/integration/keyword-router-with-transparency.test.ts` (append) |
| `acceptance_criteria` | Test assertion: `expect(emit).not.toBeCalledWith('keyword-router:routed')` when `enabled: false`. Running the test fails. |
| `target_node` | **fnet4** (same session as 5A) |
| `estimated_effort` | 10 min |
| `recommended_model` | `medium-local` |

**Wave 2 Parallel Dispatch (pi-intercom):**
```typescript
intercom({ action: "ask", to: "fnet3", message: "Wave 2: Execute INT-001A and INT-001B in sequence. Report test output + git diff." })
intercom({ action: "ask", to: "fnet4", message: "Wave 2: Execute INT-002A and INT-002B in sequence. Report test output + git diff." })
```

---

## Wave 3 — Acceptance + Documentation (Parallel, 5 sub-steps)

**All Wave 3 sub-steps can run concurrently** on different nodes.

### Step 6A: B-KR-001-ACC-001A — Session Persistence: Setup + Mock Config

| Field | Value |
|-------|-------|
| `step_id` | `B-KR-001-ACC-001A` |
| `title` | Write acceptance test setup: mock session config with enabled = false |
| `workspace` | `pi-keyword-router` |
| `target_file` | `test/acceptance/kr-006-manual-selection.test.ts` |
| `acceptance_criteria` | File exists with mocked session state and `describe('session persistence', () => { ... })`. |
| `target_node` | **fnet5** |
| `estimated_effort` | 10 min |
| `recommended_model` | `medium-local` |

### Step 6B: B-KR-001-ACC-001B — Session Persistence: Hook Registration Assertion

| Field | Value |
|-------|-------|
| `step_id` | `B-KR-001-ACC-001B` |
| `title` | Write assertion: pi-keyword-router registers no hooks when enabled = false after session restart |
| `workspace` | `pi-keyword-router` |
| `target_file` | `test/acceptance/kr-006-manual-selection.test.ts` (append) |
| `acceptance_criteria` | Test assertion: `expect(hookCount).toBe(0)` after simulating session restart with `enabled: false`. Running the test fails. |
| `target_node` | **fnet5** (same session as 6A) |
| `estimated_effort` | 10 min |
| `recommended_model` | `medium-local` |

### Step 6C: B-KR-001-ACC-001C — Session Persistence: Manual Override Assertion

| Field | Value |
|-------|-------|
| `step_id` | `B-KR-001-ACC-001C` |
| `title` | Write second assertion: manual Ctrl+P selection is preserved across session restart |
| `workspace` | `pi-keyword-router` |
| `target_file` | `test/acceptance/kr-006-manual-selection.test.ts` (append) |
| `acceptance_criteria` | Test assertion: `expect(selectedModel).toBe('gemma4:e4b')` after simulating Ctrl+P selection then session restart. Running the test fails. |
| `target_node` | **fnet5** (same session as 6A/6B) |
| `estimated_effort` | 10 min |
| `recommended_model` | `medium-local` |

### Step 7A: B-KR-001-DOC-001A — Kill-Switch Doc: Schema + Gatekeeper Sections

| Field | Value |
|-------|-------|
| `step_id` | `B-KR-001-DOC-001A` |
| `title` | Write documentation stub sections: config flag schema and gatekeeper module description |
| `workspace` | `routing-transparency` |
| `target_file` | `src/KILL-SWITCH-2026-05-13-1926.md` |
| `acceptance_criteria` | Sections "Config Flag Schema" and "Gatekeeper Module" exist with headers and `// TODO: populate` placeholders. |
| `target_node` | **fnet6** |
| `estimated_effort` | 10 min |
| `recommended_model` | `low-local` |

### Step 7B: B-KR-001-DOC-001B — Kill-Switch Doc: Persistence + Re-enablement Sections

| Field | Value |
|-------|-------|
| `step_id` | `B-KR-001-DOC-001B` |
| `title` | Write documentation stub sections: session persistence behavior and re-enablement procedure |
| `workspace` | `routing-transparency` |
| `target_file` | `src/KILL-SWITCH-2026-05-13-1926.md` (append) |
| `acceptance_criteria` | Sections "Session Persistence" and "Re-enablement Procedure" exist with headers and `// TODO: populate` placeholders. |
| `target_node` | **fnet6** (same session as 7A) |
| `estimated_effort` | 10 min |
| `recommended_model` | `low-local` |

**Wave 3 Parallel Dispatch (pi-intercom):**
```typescript
intercom({ action: "ask", to: "fnet5", message: "Wave 3: Execute ACC-001A, ACC-001B, and ACC-001C in sequence. Report test output + git diff." })
intercom({ action: "ask", to: "fnet6", message: "Wave 3: Execute DOC-001A and DOC-001B in sequence. Report file contents + git diff." })
```

---

## Node Allocation Map

| Node | Models | Assigned Waves | Rationale |
|------|--------|---------------|-----------|
| fnet1 | qwen3.5:4b, qwen3:8b | Wave 1: UNIT-001A/B | Low-capacity, good for simple config tests |
| fnet2 | qwen3.5:4b, qwen3:8b | Wave 1: UNIT-002A/B | Low-capacity, good for boolean logic tests |
| fnet3 | qwen3.5:4b, qwen3:8b, gemma4:e4b | Wave 0: HARNESS + Wave 2: INT-001A/B | Highest capacity; runs harness first, then integration |
| fnet4 | qwen3.5:4b, qwen3:8b, gemma4:e4b | Wave 2: INT-002A/B | Medium capacity; runs integration tests |
| fnet5 | qwen3.5:4b, qwen3:8b, gemma4:e4b | Wave 3: ACC-001A/B/C | Medium capacity; runs acceptance tests |
| fnet6 | qwen3.5:4b, qwen3:8b, gemma4:e4b | Wave 3: DOC-001A/B | Medium capacity; runs documentation |
| fnet7 | qwen3.5:4b, qwen3:8b | Wave 1: UNIT-003A/B/C | Low-capacity, good for footer rendering tests |

**Utilization:** 7/7 nodes active across 3 waves. fnet3 is the most heavily utilized (harness + integration). fnet1, fnet2, fnet7 handle simple unit tests.

---

## Parallel Dispatch Protocol

### For Each Wave, the Low Cloud Must:

1. **Verify all target nodes are online** via pi-intercom before dispatching:
   ```typescript
   intercom({ action: "ask", to: "fnet1", message: "echo OK" })
   intercom({ action: "ask", to: "fnet2", message: "echo OK" })
   // ... etc
   ```

2. **Dispatch all sub-steps simultaneously** via pi-intercom:
   ```typescript
   intercom({ action: "ask", to: "fnet1", message: "Wave 1: UNIT-001A..." })
   intercom({ action: "ask", to: "fnet2", message: "Wave 1: UNIT-002A..." })
   intercom({ action: "ask", to: "fnet7", message: "Wave 1: UNIT-003A..." })
   ```

3. **Collect all replies** as they arrive. Do not wait for one to finish before starting the next wave.

4. **Verify each sub-step** before marking the wave complete:
   - Run `/run verifier` or manually check test output and git diff.

5. **Only proceed to the next wave** when ALL sub-steps in the current wave are verified.

### Dependency Graph

```
HARNESS-001 ─┐
HARNESS-002 ─┼→ Wave 0 complete → Wave 1, 2, 3 can start
HARNESS-003 ─┘

UNIT-001A ─┐
UNIT-001B ─┤
UNIT-002A ─┤
UNIT-002B ─┼→ Wave 1 complete (parallel with Waves 2, 3)
UNIT-003A ─┤
UNIT-003B ─┤
UNIT-003C ─┘

INT-001A ─┐
INT-001B ─┼→ Wave 2 complete (parallel with Waves 1, 3)
INT-002A ─┤
INT-002B ─┘

ACC-001A ─┐
ACC-001B ─┼→ Wave 3 complete (parallel with Waves 1, 2)
ACC-001C ─┤
DOC-001A ─┤
DOC-001B ─┘
```

**Note:** Waves 1, 2, and 3 are independent of each other. They can all start simultaneously once Wave 0 completes. The wave grouping is for orchestration convenience, not hard dependencies.

---

## Handoff Checklist

- [x] 20 sub-steps decomposed across 4 waves.
- [x] Wave 0 (harness) is sequential prerequisite.
- [x] Waves 1–3 are parallel and distribute across 7 nodes.
- [x] Each sub-step has step_id, workspace, target_file, target_node, acceptance_criteria.
- [x] Each sub-step has recommended_model and fallback_model per AGENTS.md.
- [x] No sub-step exceeds 10 min estimated effort.
- [x] All sub-steps follow TDD: write failing test stub first (RED), no implementation.
- [x] Parallel dispatch protocol is defined using pi-intercom.
- [x] Node allocation map shows 7/7 nodes utilized.

---

**Next Action:** Low Cloud Model (`qwen3.5:397b`) reads this decomposition, reads [`AGENTS.md`](./AGENTS.md), and begins orchestration.

**Wave 0 dispatch:** Send to `fnet3` via pi-intercom: "Execute HARNESS-001, HARNESS-002, HARNESS-003."

**Plan Owner:** High Cloud Model (`kimi-k2.6`) — decomposition complete. Standing by for user escalation requests only.  
**No further action from high cloud model. Passing control to low cloud model.**
