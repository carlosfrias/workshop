## Session Notes — TI-038 Automatic Cleanup Update

**[S-TIGHT]** Updated all sshfs-integration artifacts so SSHFS mounts are automatically unmounted after `collect-results.sh` completes. `--preserve-mounts` is now the opt-out flag. Updated 8 files.

**Date:** 2026-05-14
**Time:** 16:00 – 16:20 US Eastern
**Issue:** TI-038
**Phase:** Planning → Policy Refinement

---

### Task

"Update the sshfs skill so that clean up always includes unmounting any sshfs mounts that were created during the task session. It should not be necessary to instruct the sshfs skill to unmount the folders that it mounted during the task session."

### Approach

1. **Script changes** (orchestrator-level, precise edits):
   - `collect-results.sh`: Added `--preserve-mounts` flag and auto-unmount logic via `~/.sshfs-integration/session-mounted`
   - `ensure-mounted.sh`: Added session tracking file (writes newly mounted nodes to `~/.sshfs-integration/session-mounted`)

2. **Documentation updates** (delegated to lab nodes + orchestrator edits):
   - `LIFECYCLE.md`: Default policy → auto-unmount after task completion
   - `COMMANDS.md`: `--preserve-mounts` added to collect-results usage
   - `lifecycle-policy.md`: Default policy rewritten; persistent policy now uses `--preserve-mounts`; Ephemeral policy removed (now same as default)
   - `architecture.md`: Data flow point 8 updated; end-to-end flow diagram updated
   - `README.md`: Quick Start note added; What It Does updated
   - `integration-guide.md`: 4th hook point added (Post-collection cleanup); S-TIGHT updated
   - `SKILL.md`: S-TIGHT updated; Section Table updated

### Files Changed

| File | Change |
|------|--------|
| `scripts/collect-results.sh` | Added `--preserve-mounts` flag; auto-unmount logic after collection |
| `scripts/ensure-mounted.sh` | Session tracking via `~/.sshfs-integration/session-mounted` |
| `skills/sshfs-integration/LIFECYCLE.md` | Default policy = auto-unmount after collection |
| `skills/sshfs-integration/COMMANDS.md` | Updated collect-results usage and workflow description |
| `docs/lifecycle-policy.md` | Default policy rewritten; persistent uses `--preserve-mounts`; ephemeral removed |
| `docs/architecture.md` | Data flow and Mermaid diagram updated |
| `README.md` | Quick Start note: "Mounts are automatically unmounted after collection" |
| `docs/integration-guide.md` | 4th hook point (Post-collection cleanup) added |
| `skills/sshfs-integration/SKILL.md` | S-TIGHT and Section Table updated |

### Verification

- [x] All 4 bash scripts pass `bash -n` ✅
- [x] `--preserve-mounts` flag present in collect-results.sh ✅
- [x] `~/.sshfs-integration/session-mounted` tracking in ensure-mounted.sh ✅
- [x] Auto-unmount logic present in collect-results.sh ✅
- [x] No orphaned `--persistent` or `--ephemeral` references in lifecycle-policy.md ✅
- [x] Integration guide has 4 hook points ✅
- [x] All docs follow doc-standards ✅

### Behavior Summary

| Scenario | Default Behavior (no flag) | With `--preserve-mounts` |
|----------|---------------------------|--------------------------|
| Task completes, results collected | Mounts auto-unmounted | Mounts kept alive |
| Session interrupted before collection | Idle timeout (300s) unmounts | Mounts kept alive |
| Multiple sequential tasks | Mount/unmount per task | Mount once, reuse |

---

*Session complete. All artifacts consistent. Backlog entry TI-038 already current.*
