# Status: Phase 1B — Runtime Complexity Logging (Complete)

**Date:** 2026-05-01 19:31  
**Session:** Wiring meta-framework orchestration (Phase 1B)  
**Files changed:** 7 files across `extensions/pi-keyword-router/` and `scripts/`

---

## What Changed

### Extension: Auto-Complexity Routing Fixes (from prior session)

**Bug found during testing:** The extension wasn't loading at all because `index.ts` imported `ModelRouter` but `lib/index.ts` only exported `KeywordRouter`. This was a pre-existing rename bug from when the extension was forked from `pi-model-router`.

| Fix | File |
|-----|------|
| `ModelRouter` → `KeywordRouter` import | `index.ts` |
| `satisfies` → `as` type assertions | `index.ts` |
| `structuredClone` → `JSON.parse(JSON.stringify)` | `lib/config.ts` |
| Added diagnostic `console.log` on load | `index.ts` |
| Wrapped extension body in `try/catch` | `index.ts` |

**Test result:** After fixes, extension loads correctly and routes prompts by complexity. Verified with "Check if fnet2 is online" → `trivial` → `qwen3.5:4b`.

---

### Phase 1B: Runtime Complexity Logging

**Goal:** Every routing decision is automatically logged for cost/quality analysis.

**New file: `lib/logger.ts`**
- `logRoutingDecision()` — appends routing metadata to JSONL file (append-safe, no races)
- `readRoutingLog()` — reads all entries from JSONL
- Cost lookup map: local = $0, cloud = ~$0.01/prompt
- Prompt hash + preview stored (not full prompt, for privacy)
- Log schema: `{timestamp, prompt_hash, prompt_preview, route_name, model, provider, complexity, confidence, latency_ms, cost, source, prev_model, prev_provider}`

**Updated: `index.ts`**
- Measures classification latency (`Date.now()` before/after `router.classify()`)
- Runs `heuristicClassify()` independently to capture complexity + confidence
- Calls `logRoutingDecision()` after every routing decision
- Also logs manual model selections (source = "manual")
- New command: `/keyword-route-log` — shows last 10 routing decisions from JSONL

**Updated: `lib/index.ts`**
- Exported `logRoutingDecision`, `readRoutingLog`, `RoutingLogEntry`

**Updated: `scripts/routing_dashboard.py`**
- Reads from both `model-routing-decisions.jsonl` (extension) and `model-performance-log.json` (manual)
- Merges and deduplicates by timestamp
- New sections: "By Source", "Avg heuristic confidence", "Low confidence warnings"

**Updated: `scripts/performance_logger.py`**
- `merge_logs()` — reads from both JSONL and legacy JSON
- `--list` and `--report` now show unified view of all routing decisions

**Updated: `README.md`**
- Documented `/keyword-route-log` command

---

## Log File Location

```
<workspace>/technical-infrastructure/wiki/sessions/model-routing-decisions.jsonl
```

One JSON object per line, append-safe, readable by both TypeScript and Python.

---

## Test Validation

Created 5 test entries in JSONL, ran dashboard:

| Metric | Result |
|--------|--------|
| Total prompts | 5 (from JSONL) + 3 (from legacy JSON) = 8 |
| Avg latency | 5-10ms for extension classification |
| Avg heuristic confidence | 0.79 |
| Cloud prompts | 1 ($0.01) |

Dashboard correctly identifies:
- TRIVIAL: 3 prompts (38%)
- SIMPLE: 2 prompts (25%)
- MEDIUM: 1 prompt (12%)
- HARD: 2 prompts (25%)

---

## Commands Available

| Command | Purpose |
|---------|---------|
| `/keyword-route` | Show router state, routes, session history |
| `/keyword-route-log` | Show recent routing decisions from JSONL log |
| `/keyword-route-off` | Disable auto-routing |
| `/keyword-route-on` | Re-enable auto-routing |

---

## Remaining Work (from PLAN-2026-05-01-1730.md)

| Phase | Task | Priority |
|-------|------|----------|
| **1C** | LLM fallback for borderline prompts (low-confidence → Python classifier) | 🟡 Optional safety net |
| **2A** | Auto-decomposition on HARD classification (trigger `decompose_task.py`) | 🔴 Critical — core orchestration value |
| **2B** | Adaptive feedback loop (`adaptive_feedback.py` auto-updates rules) | 🟡 Important for long-term |
| **2C** | Extension release v1.1.0 (commit, tag, push) | 🔴 Critical — make 1A+1B permanent |

**Critical path:** 2A → 2C = ~4-6 hours

---

## Quality Checklist

- [x] **Document session activity:** This STATUS file
- [x] **Before deleting ephemeral files:** Test JSONL removed
- [x] **Update backlog:** PLAN-2026-05-01-1730.md updated
- [x] **Capture session narrative:** Key bug found (ModelRouter→KeywordRouter) and fixed
- [x] **Update WIKI.md:** N/A — no new terminology
- [x] **Test before declaring complete:** Dashboard and logger validated with test data
- [x] **API connectivity confirmed:** N/A — local code changes only
- [x] **Secrets in env vars:** N/A — no secrets in code
- [x] **Config changes logged:** N/A — no config changes
- [x] **For publishing tasks:** README updated, code tested

---

## Next Actions

1. **Test in live session:** Send a few prompts across complexity tiers and run `/keyword-route-log` to verify entries appear
2. **Start Phase 2A:** Auto-decomposition on HARD classification
