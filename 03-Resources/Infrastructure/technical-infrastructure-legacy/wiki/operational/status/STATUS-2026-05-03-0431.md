# STATUS-2026-05-03-0431

## Activity
Created Ansible playbook for graceful lab-node shutdown.

## Files Changed
- `technical-infrastructure/ansible/playbooks/shutdown-lab-nodes.yml`

## Verification
- [x] `ansible-playbook --syntax-check` passed
- [ ] Live `--check` (queued — requires nodes online)
- [ ] Live execution (queued — requires nodes online)

## Context
Ad-hoc shutdown of fnet1–fnet7 was executed prior to playbook creation. All nodes confirmed UNREACHABLE via `ansible -m ping`. The new playbook uses `nohup bash -c 'sleep 5 && shutdown now'` to give Ansible time to record task success before the SSH connection drops.

## Next Steps
1. Power nodes back on
2. Run `ansible-playbook --check`
3. Run live playbook test
4. If test passes, mark complete
