# STATUS-2026-05-02-2145

**Session:** fnet2/fnet7 fixes + Ctrl+P cache cleanup
**Domain:** technical-infrastructure
**Started:** 2026-05-02 21:45 ET

---

## Completed

### 1. fnet2 NVIDIA Driver Fix (TI-016 Issue #1)
- **Root cause:** NVIDIA 595.71 driver installed on Intel UHD 630 + GTX 660. `nvidia-smi` hung during hardware detection.
- **Fix:** Removed all nvidia-* packages, blacklisted nouveau, rebooted.
- **Verification:** Task `8093bed8` submitted via TI-009, completed successfully. GPU detection now works.
- **Status:** ✅ Complete

### 2. fnet7 Performance Fix (TI-016 Issue #4)
- **Root cause:** CPU governor set to `powersave` (22-31% slower than fnet3–fnet6).
- **Fix:** Changed governor to `performance`, installed `cpufrequtils`, enabled systemd service.
- **Verification:** Task `4dd77270` submitted via TI-009. Task worker on fnet7 had timer issues — task remained in `running/` state. Light task `4dd77270` still pending execution.
- **Status:** 🟡 Fix applied, verification incomplete (task worker timing issue on fnet7)

### 3. Ctrl+P Model Cache Cleanup (TI-021)
- **Root cause:** `~/.pi/agent/cache/ollama-models.json` had 39 cached cloud models. Ctrl+P merged local + configured + cache = 39+ entries.
- **Fix:** Created `scripts/prune-cloud-model-cache.py` that matches cache keys against `models.json` (handling `:cloud` suffix mismatch). Pruned 36 models, kept 3 configured ones.
- **Status:** ✅ Complete. Restart pi to see cleaned picker.

### 4. AGENTS.md Size Reduction (TI-027)
- **Root cause:** Root `AGENTS.md` was 8,542 bytes — too large for low models.
- **Fix:** Split into `AGENTS.md` (2,766 bytes, ~691 tokens) and `AGENTS-full.md` (7,495 bytes). Added Framework Readiness Check section.
- **Status:** ✅ Complete

### 5. Framework Bypass Root Cause (TI-026)
- **Root cause:** `pi-keyword-router` extension cloned but not symlinked to `~/.pi/agent/extensions/`. Extension loader never discovered it.
- **Fix:** Created symlink `~/.pi/agent/extensions/pi-keyword-router → ~/.pi/agent/git/github.com/.../pi-keyword-router`.
- **AGENTS.md updated:** Added "Must Always" rule requiring framework verification, "Must Never" rule forbidding direct SSH for multi-node work, Framework Readiness Check section.
- **Status:** ✅ Complete

---

## Incomplete / Next Steps

| Item | Priority | Action |
|------|----------|--------|
| fnet7 task worker timing | Medium | Investigate why fnet7 task worker didn't complete the verification task. Timer fires but task stayed in `running/`. |
| fnet7 Ollama update | Low | fnet7 still on 0.21.1 (vs 0.22.1 on fnet3–fnet6). Update via `curl -fsSL https://ollama.com/install.sh \| sudo bash` |
| fnet7 benchmark verification | Low | After Ollama update, re-run benchmark to confirm ~22% speedup from governor fix |
| fnet2 full detection + benchmark | Low | Re-run `remote-detect.sh` + `benchmark-lab.sh` for fnet2 to generate profiles |

## Test Results

| Test | Result |
|------|--------|
| `classify_prompt.py --help` | ✅ Pass |
| `ti011_node_registry.py --help` | ✅ Pass |
| `submit_task.py --help` | ✅ Pass |
| fnet2 task execution | ✅ Completed (8093bed8) |
| fnet7 task execution | 🟡 Stuck in running/ |
| Cache prune script | ✅ 39 → 3 models |
| Extension symlink | ✅ Created |

## Files Modified/Created

- `AGENTS.md` — Slimmed to 2,766 bytes, added Framework Readiness Check
- `AGENTS-full.md` — Created with full docs
- `technical-infrastructure/AGENTS.md` — Added framework rules + readiness check
- `technical-infrastructure/scripts/prune-cloud-model-cache.py` — Cache cleanup script
- `technical-infrastructure/tasks/fnet2-verify-detection.json` — Task definition
- `technical-infrastructure/tasks/fnet7-verify-*.json` — Task definitions

## Session Narrative

See `SESSION-NOTES-2026-05-02-2145.md` for detailed steps and pivots.

## Backlog Updates

- TI-021: Moved from 📋 BACKLOG → ✅ Complete
- TI-026: Moved from 🟡 IN PROGRESS → ✅ Complete (extension installed)
- TI-016 Issue #1 (fnet2): ✅ Closed
- TI-016 Issue #4 (fnet7): 🟡 Partial (fix applied, verification pending)

---
**END STATUS**
