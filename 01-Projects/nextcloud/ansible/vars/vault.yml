---
# NextCloud Vault - Encrypted Variables
# Create with: ansible-vault encrypt vars/vault.yml
# Edit with:  ansible-vault edit vars/vault.yml
#
# This file contains placeholder values.
# BEFORE deploying, replace with real passwords and encrypt:
#   ansible-vault encrypt vars/vault.yml

nextcloud_admin_password: "CHANGE_ME_admin_password"
nextcloud_db_password: "CHANGE_ME_db_password"
nextcloud_db_root_password: "CHANGE_ME_db_root_password"